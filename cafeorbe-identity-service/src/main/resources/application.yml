spring:
  application:
    name: cafeorbe-identity-service

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:cafeorbe_identity}
    username: ${DB_USER:cafeorbe}
    password: ${DB_PASSWORD:cafeorbe}

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:cafeorbe}
    password: ${RABBITMQ_PASSWORD:cafeorbe}

server:
  port: ${SERVER_PORT:8081}

jwt:
  secret: ${JWT_SECRET:}
  expiration-minutes: ${JWT_EXPIRATION_MINUTES:60}

management:
  endpoints:
    web:
      exposure:
        include: health,info
